Use the **Analyst** sub-agent to begin requirements elicitation.

The agent will engage with the user through structured phases to produce `spec/REQUIREMENTS.md`.

**Error handling:** If the Analyst agent fails to start or returns an internal error, stop immediately, report the failure to the user, and wait for instructions before retrying.
